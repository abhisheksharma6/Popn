<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_sign_in_up"
    android:layout_width="match_parent"
    android:orientation="vertical"
    android:layout_gravity="center"
    android:gravity="center"
    android:background="@color/QRCodeWhiteColor"
    android:layout_height="match_parent"
    tools:context="com.popn.PopActivities.SignInUPActivity">

   <Button
       android:layout_width="match_parent"
       android:background="@drawable/login_edit_box_border"
       android:layout_marginRight="20dp"
       android:layout_marginLeft="20dp"
       android:text="Signup"
       android:id="@+id/signup"
       android:textSize="15sp"
       android:layout_gravity="center"
       android:gravity="center"
       android:textColor="@color/colorTextPink"
       android:layout_height="35dp" />
    <Button
       android:layout_width="match_parent"
       android:background="@drawable/login_edit_box_border"
       android:layout_marginTop="7dp"
       android:text="Login"
        android:layout_marginRight="20dp"
        android:layout_marginLeft="20dp"
       android:textSize="15sp"
        android:id="@+id/login"
       android:layout_gravity="center"
       android:gravity="center"
       android:textColor="@color/colorTextPink"
       android:layout_height="35dp" />
</LinearLayout>
